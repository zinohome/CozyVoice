voice:
  stt:
    provider: mock
    openai:
      api_key: ${OPENAI_API_KEY}
      base_url: ${OPENAI_BASE_URL:-}   # 空则走官方
      model: whisper-1

  tts:
    provider: mock
    default_voice_id: "101001"
    default_format: wav
    tencent:
      secret_id: ${TENCENT_SECRET_ID}
      secret_key: ${TENCENT_SECRET_KEY}
      region: ${TENCENT_REGION:-ap-guangzhou}

  brain:
    base_url: ${BRAIN_URL:-http://localhost:8000}
    timeout: 60.0

  api:
    host: ${VOICE_HOST:-0.0.0.0}
    port: ${VOICE_PORT:-8002}

  realtime:
    provider: openai_realtime
    openai:
      api_key: ${OPENAI_API_KEY}
      base_url: ${OPENAI_REALTIME_BASE_URL:-}   # 独立变量；多数代理不支持 Realtime，留空走官方 wss
      model: ${OPENAI_REALTIME_MODEL:-gpt-4o-realtime-preview}
    # livekit 段留给 γ（TODO）：
    # livekit:
    #   url: ${LIVEKIT_URL}
    #   api_key: ${LIVEKIT_API_KEY}
    #   api_secret: ${LIVEKIT_API_SECRET}
